<?php

// DB CONSTANT
// We have redundant data because we are switching to Doctrine
// Note: utf8mb4 charset is enforced in EntityManagerFactory::getEntityManager()
define('DB_CONSTANTS', array(
    'host'=>"localhost",
    'user'=>"root",
    'psw'=>"",
    'db'=>"",
    'password'=>"",
    'dbname'=>"",
    'driver' => 'mysqli',
    'charset' => 'utf8mb4',
));


